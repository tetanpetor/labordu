function format(timeFormat) {
    // Assume this function formats the time based on the given format string
    return "10 AM"; // Example output for demonstration
}

// Assigning the formatted hour to the formatHour variable
var formatHour = format("%I %p");

console.log(formatHour); // Output: "10 AM"
