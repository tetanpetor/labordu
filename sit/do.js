} else if (!sort.field && sort.op !== 'count') {
    // If the sort field is not specified and the operation is not 'count'
    // Perform the logic for handling this scenario
    // For example, you may choose to apply a default sorting behavior or throw an error
}
